experiment_name: aesthetic_resnet18_linear_real_v1
candidate_name: aesthetic_resnet18_linear_real_v1

task: aesthetic

dataset:
  manifest_uri: swift://training-module-proj03/data/aesthetic_ava/manifests/aesthetic_manifest_v1.jsonl
  dataset_version: aesthetic-ava-v1
  start_index: 0
  max_records: 30000
  subset_seed: 42

model:
  type: resnet18_linear
  family: linear_head
  version: aesthetic-resnet18-linear-v1
  image_size: 224
  pretrained: true

training:
  batch_size: 2
  epochs: 5
  learning_rate: 0.0005
  weight_decay: 0.0001
  resume: auto
  advance_chunk_on_resume: false
  log_every_n_batches: 100

runtime:
  device: auto
  seed: 42
  num_workers: 2

checkpoint:
  root_dir: cache/checkpoints

output:
  artifact_dir: swift://training-module-proj03/artifacts/aesthetic/resnet18_linear_real_v1
  save_history: true

storage:
  backend: object_store

evaluation:
  metrics: [mse, mae]