apiVersion: apps/v1
kind: Deployment
metadata:
  name: serving-nginx
  namespace: photoprism
spec:
  replicas: 1
  selector:
    matchLabels:
      app: serving-nginx
  template:
    metadata:
      labels:
        app: serving-nginx
    spec:
      containers:
        - name: nginx
          image: nginx:alpine
          ports:
            - containerPort: 80
          volumeMounts:
            - name: nginx-config
              mountPath: /etc/nginx/nginx.conf
              subPath: nginx.conf
      volumes:
        - name: nginx-config
          configMap:
            name: nginx-config
---
apiVersion: v1
kind: Service
metadata:
  name: serving-nginx
  namespace: photoprism
spec:
  selector:
    app: serving-nginx
  ports:
    - port: 80
      targetPort: 80
  type: LoadBalancer
